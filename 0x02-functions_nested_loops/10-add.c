#include "main.h"
/**
* add -  a function that adds two integers
* and returns the result.
* @a: parameter function
* @b: parameter function
* Return: a + b
*/

int add(int a, int b)
{
	return (a + b);
}
