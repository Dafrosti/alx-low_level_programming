#include <stdio.h>

/**
 * main - Uses printf funtion to print "with proper grammer,
 * but the outcome is a piece of art, followed by a new line
 * Return: The Integer 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
